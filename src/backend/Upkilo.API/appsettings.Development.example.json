{
  "//": [
    "Local development overrides. Loaded only when ASPNETCORE_ENVIRONMENT=Development,",
    "so nothing here reaches staging or production — those substitute ${DATABASE_URL}",
    "into appsettings.Production.json at deploy time.",
    "",
    "Why this file exists: appsettings.json defaults DefaultConnection to port 6432,",
    "which is PgBouncer. PgBouncer's pinned image does not exist on Docker Hub, so that",
    "port has never been served locally, and the README quick start ('dotnet ef database",
    "update') failed with 'Failed to connect to 127.0.0.1:6432' on a fresh clone.",
    "",
    "These values match the postgres service in docker-compose.yml exactly — port 5432,",
    "database upkilo_dev — and the connection string the integration tests already use."
  ],
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=upkilo_dev;Username=upkilo;Password=upkilo_dev_password;Pooling=true;Maximum Pool Size=100;Minimum Pool Size=5",
    "Redis": "127.0.0.1:6379,password=upkilo_dev_redis_local,abortConnect=false",
    "ReplicaConnection": "Host=localhost;Port=5432;Database=upkilo_dev;Username=upkilo;Password=upkilo_dev_password;Pooling=true;Maximum Pool Size=20;Minimum Pool Size=2;ApplicationName=Upkilo-ReadReplica"
  },
  "ConnectionStrings_Redis_note": "docker-compose starts redis with --requirepass (default upkilo_dev_redis_local), but appsettings.json's Redis string carries no password, so the API fails Redis auth locally and any endpoint touching cache or rate limiting returns 500.",
  "Cors": {
    // 3000 is the documented default. 3100 is here because a second app commonly
    // squats 3000 on a dev machine, and a frontend on any other port gets an opaque
    // "Network Error" in the browser — CORS rejection, not an API fault, which is a
    // slow thing to diagnose from the client side.
    "AllowedOrigins": [
      "http://localhost:3000",
      "https://localhost:3000",
      "http://localhost:3100",
      "https://localhost:3100"
    ]
  },
  "Jwt": {
    // The API refuses to issue or validate a token without this and returns 400 on
    // every request, including /health. Dev-only value: production supplies it from
    // Key Vault via the Jwt__Secret environment variable.
    "Secret": "dev-only-jwt-secret-min-32-chars-DO-NOT-USE-IN-PRODUCTION-REPLACE-ME"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
